name: Trigger Explorer Deploy

on:
  push:
    branches: [main]
    paths:
      - 'incidents/**'

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -s -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
